<nav class="app-header navbar navbar-expand bg-dark-gray shadow">
    <div class="container-fluid">
        <ul class="navbar-nav">
            <li class="nav-item user-menu mt-1 ms-2">
                <a asp-action="Index" asp-asp-controller="Home">
                    <img src="~/logo_alter.png" alt="BrainFlow Logo" class="brand-logo">
                </a>
            </li>
            @if (User.IsInRole("2"))
            {
                <li class="nav-item user-menu ms-4 mt-1">
                    <a class="nav-link btn btn-primary rounded-pill shadow" asp-area="Afiliado" asp-controller="Home" asp-action="Index">
                        <span class="fas fa-handshake me-2"></span>Painel do Afiliado</a>
                </li>
            }
        </ul>
        <ul class="navbar-nav">
            @if (User.Identity.IsAuthenticated)
            {
                <li class="nav-item user-menu me-2 mt-2">
                    <span class="d-none d-md-inline text-white me-4" style="font-size: 1.1em;">
                        <span class="fas fa-user me-2"></span>@User.Identity.Name</span>
                </li>
                <li class="nav-item user-menu me-3 mt-1">
                    <a asp-controller="Conta" asp-action="Logout" type="button" class="btn btn-red shadow text-white py-1 px-2 fw-light">
                        sair<i class="ms-2 fas fa-arrow-alt-circle-right"></i>
                    </a>
                </li>
            }
            else
            {
                <li class="nav-item user-menu me-3 mt-1">
                    <a class="nav-link btn btn-green rounded-pill shadow" asp-controller="Conta" asp-action="Cadastro">
                        <span class="fas fa-plus me-2"></span>Cadastrar
                    </a>
                </li>
                <li class="nav-item user-menu me-3 mt-1">
                    <a class="nav-link btn btn-primary rounded-pill shadow" asp-controller="Conta" asp-action="Login">
                        <span class="fas fa-check me-2"></span>Login
                    </a>
                </li>
            }
        </ul>
    </div>
</nav>